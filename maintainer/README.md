# Maintainer-only files

`mcp.json` and convert/Knowledge MCP sources are kept for future work. They are **not** part of the Team Marketplace plugin install surface. Do not move `mcp.json` back to the repo root until product is ready to ship an MCP again (Cursor auto-discovers root `mcp.json`).
